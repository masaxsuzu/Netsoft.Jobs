# Netsoft.Jobs

単一コンピュータ上で動作する、長時間実行 Job の共通実行基盤。
Job の登録・監視・キャンセルができる。動かし方は [README](./README.md) を参照。

## 開発サイクル

1. **依頼** — 利用者が自然言語で伝える。粒度は問わない
2. **タスク化** — Claude が実装単位に分解し、**何を作るかについて着手前に合意を取る**。
   粒度は「1 PR で完結し CI が通る」。TaskCreate / TaskUpdate で管理する。
   **どう作るかは合意の対象ではない**（下の「何を聞き、何を聞かないか」）
3. **実装** — サブエージェントに 1 タスクずつ渡す。受け入れ条件・触ってよいファイルの範囲・
   従う規約・**build と test を通すまでがタスク**であることを明記する。
   並行させるのはファイルが重ならない場合のみ
4. **PR** — 1 タスク = 1 PR。**`/ship` を使う**。`.github/pull_request_template.md` を埋める。
   ゲート（`tools/gate.sh`）を通っていないコミットは push できない（`tools/gate-hook.sh` が止める）
5. **CI と修正** — 落ちたら診断して修正を push。放置して次のタスクへ進まない。
   原因が main 側なら PR にコメントして main を先に直す
6. **マージ** — ルールセットが CI を必須チェックにしており、auto-merge を有効にした PR は
   CI 通過後に GitHub が squash merge する
7. **測る** — 10 PR ごとに `/retro`。規則が効いているかを数えて、効いていないものを直す。
   **規則は意見で変えない**（この節の記述はどれも #68 以降、数えてから書き直している）

### 何を聞き、何を聞かないか

聞いてよいのは**利用者から見えること**だけ。画面に何が出るか、どのボタンが押せるか、
壊れたときどう見えるか、何を捨ててよいか。**実装の選び方は聞かない** ── 決めて、
置いた仮定を PR の「レビューで見てほしい点」に書く。読んで違っていれば直せばよい。

判定はこう。**答えが画面や API の見え方を変えるなら聞く。コードの形しか変えないなら聞かない。**

かつては「着手前に利用者の合意を取る。勝手に始めない」だった。#67 では、コードを 1 行書く前に
**4 往復している**。うち「handler が居ない状態を誰が確定させるか」は実装の選び方で、
聞くべきではなかった（実際「直前の提案を無しに。やり直し」と差し戻され、
次の返答で「実装は任せます。ユーザー視点の条件で足りない条件は質問しろ」と明示された）。
同じ回の「`Resuming` は画面に見えるべきか」は聞いてよい質問で、その答えが設計を決めている。

**合意が要るのはスコープだけ**、という線を引いておかないと、往復が実装の細部まで伸びる。
細部は書いてから直す方が速く、書いてあれば読み直せる。

### auto-merge と needs-review

受け入れ条件を満たし、ローカルで build / test / format が通っているなら、**既定は auto-merge**。

`needs-review` を付けるのは、**その変更が今後の判断を縛る規則を新しく決めているとき**だけ。
次のどれかに当たるかで見る。

- **既存の決定を覆す** — 前に「こうする」と決めた理由を、別の理由で置き換えている
- **利用者にしか決められない選び方を含む** — どこまで畳んでよいか、何を画面に見せるか、
  どこで妥協するか。実装の都合では決まらず、依頼にも書かれていなかったもの
- **外から見える約束を変える** — API の文字列、状態の増減、既存データの意味

当てはまらないなら auto-merge にする。**大きい・難しい・時間がかかったは理由にならない。**

**迷ったら、迷った理由を PR の「レビューで見てほしい点」に書いてから auto-merge にする。**
ラベルは迷いの表明ではなく「利用者が決めるべきことが入っている」という申告に使う。

かつては「設計上の判断を含むなら付ける、迷ったら付けない」だった。#1〜#67 で測ると
31 件（46%）に付いており、**GitHub 上でレビューが付いたものは 1 件も無く、19 件は
10 分以内にそのままマージされた**。全部に付くラベルは「読まずに通す列」になり、
本当に見てほしいものが埋もれる。実際に差し戻しが起きたのは、上の 3 つに当たる少数だけだった
（#64 は「再利用性は Domain に限る」で適用済みの畳み込みを 2 件戻し、
#67 は要求の見え方の方針が変わって作り直している）。

## どこに何を書くか

同じことを 4 か所に書かない。読み手が探す場所が決まっている。

- **コード** — How。どうやっているか。コードだけが実際に動くので、ここに嘘は書けない
- **テストコード** — What。何が守られているか。テスト名が仕様の見出しになる
- **コミットログ** — Why。なぜ変えたか。差分からは読めない、変更を必要とした事情を残す
- **コードコメント** — Why not。**なぜそうしなかったか**。選ばなかった書き方、
  順序を入れ替えてはいけない理由、過去に試して壊れたこと。
  How をなぞるコメント（コードを読めば分かること）は書かない

## やらないこと

- `main` への直接 push。変更は必ず PR を通す（ルールセットで禁止されている）
- 自前のマージ処理。マージの判断は auto-merge を有効にするかどうかだけ
- 利用者への通知（send_later 等）。報告はチャットで行う
- デプロイ（未定。CI はビルドとテストのみ）
- **`src/` での相互排他** — `lock`・`SemaphoreSlim`・`Monitor`・`Mutex` の類。
  アナライザー (`NJ0001`) がビルドを落とす（下の節）

### src で排他を使わない

排他は「重なったら待たせる」ことで正しさを作るので、**正しさの根拠が実行の時間軸に乗る**。
重ならなければ正しい、という形は、重なり方が変わった日に黙って壊れる。
版や不可分な差し替えで表せば、何本重なっても、どの順で終わっても結果が変わらない。

規則を先に決めて当てはめたのではない。#83 と #84 で同じ場所を 2 通りに直して、
**後から片方が要らなくなった**ので規則にしている。#83 は一覧の取り直しを直列化して
症状を消し、#84 は行に版を載せて到着順を無意味にした。#84 の後、#83 の門は
何も守っていなかった。

- `Interlocked` と `Volatile` は禁じない。相互排他ではなく順序と不可分性の指定で、
  待たせるものが無い
- 禁止は `src/` だけ。`tests/` の並行ハーネスは重なりを故意に作る道具で、
  そこの排他は検証の一部
- 例外は置いていない。置きたくなったら、それは**排他が要る設計を選んだ**ということなので、
  設計の方を先に疑う（`RunningJobRegistry` は排他が要ると書いてあったが、
  `CancellationTokenSource` を破棄しない形にしたら要らなくなった）

## ディレクトリ構造

- `src/Domain` — エンティティと状態遷移。依存ゼロ
- `src/Contracts` — プロセスをまたぐ契約。Domain のみ参照
- `src/Features` — コマンドとエンドポイント。機能ごとのフォルダ
- `src/Infrastructure` — SQLite
- `src/Web` — API + 実行エンジンのホスト
- `src/Ui` — 画面（Blazor Server）のホスト
- `tests/` — src と対応。`E2E` は 2 プロセスを実起動し、`Stress` はホストを実起動して壊し、外から同時に叩く
- `docs/` — 開発の決めごと。**作業を始める前に読むこと**

## docs

- [build.md](./docs/build.md) — ビルド・テスト・カバレッジの規則
- [conventions.md](./docs/conventions.md) — コーディング規約と命名
- [operating.md](./docs/operating.md) — 単一プロセス前提と、handler を書く人への契約
- [loop.md](./docs/loop.md) — ループの実測。`/retro` が 10 PR ごとに置き換える
