bin/
obj/
.vs/
*.user
TestResults/
artifacts/

# dotnet run が作る開発用の DB
jobs.db*

# ビルドが NuGet キャッシュからコピーするフレームワークのスクリプト
src/Ui/wwwroot/_framework/

# coverlet.msbuild 時代の名残。collector は TestResults/ にしか書かない
coverage.json

# サブエージェントが使う worktree。repo の中に作られるので、
# git add -A が丸ごと拾ってコミットに巻き込む（.claude/skills は追跡しているため
# .claude/ ごとの除外にはできない）。
.claude/worktrees/
