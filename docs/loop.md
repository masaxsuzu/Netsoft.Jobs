# ループの実測

`/retro` が 10 PR ごとに置き換える。**git の履歴がそのまま推移になる**ので、
前回との差は `git log -p docs/loop.md` で読む。

数え方は [tools/loop-stats/loop-stats.py](../tools/loop-stats/loop-stats.py)、
手順は [.claude/skills/retro/SKILL.md](../.claude/skills/retro/SKILL.md)。

## 2026-08-06  #1〜#71

| 指標 | 全体 | 直近 20 件 |
|---|---|---|
| PR | 71 件 | 20 件 |
| マージ済み | 71 / 71 (100%) | 20 / 20 (100%) |
| マージまでの中央値 | 3 分 | 2 分 |
| マージまでの最大 | 10.1 時間 | 10.1 時間 |
| needs-review | 33 / 71 (46%) | 6 / 20 (30%) |
| テンプレート充足 | 66 / 71 (93%) | 20 / 20 (100%) |
| 実測の記載 | 56 / 71 (79%) | 16 / 20 (80%) |
| needs-review の的中（GitHub 上） | 0 / 33 (0%) | 0 / 6 (0%) |

### 読み取り

- **`needs-review` の的中が 0/33。** 付けた 33 件のうち、GitHub 上でレビューが付いたものは
  1 件も無い。#68 で基準を絞って 46% → 30%（直近）まで下がったが、**まだ的中していない**。
  次の回でも 0% なら、ラベルそのものを疑う番になる
- **マージまでの中央値が 3 分。** CI が通れば即マージされている。ここは詰まっていない
- **テンプレート充足が直近 100%。** 手順の後半は通っている。#71 で push を機械で止めたので、
  次の回はこれが落ちないことを確認する
- **実測の記載が 79% で動いていない。** 「確認したこと」に数字を書かない PR が 2 割ある。
  規則としては書いてあるので、効いていない側の候補

### この回の前提

- `reviewed-by:masaxsuzu` の検索が 0 件だったため、的中の分子は 0 で確定している
- 実際の差し戻しはチャットで起きており（#64 と #67 で 1 件ずつ）、この表には出ない
